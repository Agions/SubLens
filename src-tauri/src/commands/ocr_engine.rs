// This module previously contained OCR command handlers (now deleted).
// The types (BoundingBox, OCRProcessResult, etc.) remain in types.rs
// for potential future use. This file is kept to avoid breaking imports
// that may reference these types.
